const quicksort = (array) => {
  let pivot = array[0];
  let partitionIndex;

  

  

  return array;

}
const partition = (array, pivot, left, right) => {};

const swap = (array, i, j) => {
  let temp = array[i];
  array[i] = array[j];
  array[j] = temp;
};

console.log(quicksort([4,3,2,1,0]));